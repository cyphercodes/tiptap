import { test } from '@playwright/test'

test.describe('/src/Examples/Gapcursor/Vue/', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/src/Examples/Gapcursor/Vue/')
  })

  // TODO: Write tests
})
