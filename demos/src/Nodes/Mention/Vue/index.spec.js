import { test } from '@playwright/test'

test.describe('/src/Nodes/Mention/Vue/', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/src/Nodes/Mention/Vue/')
  })

  // TODO: Write tests
})
